<Window x:Class="VoiceTool.Alert"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VoiceTool"
        mc:Ignorable="d"
        Title="Alert" Height="200" Width="300" ResizeMode="NoResize">
    <Grid >
        <TextBlock Name="AlertMessage" Text="Temp Warning" Margin="10,20,10,0"/>
        <ListBox Margin="0,102,10,10" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Disabled" Background="Transparent">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBoxItem>
                <Button Content="Ok" HorizontalAlignment="Right" Click="OKButton_Click" Margin="5,0,10,0" Width="50"/>
            </ListBoxItem>
        </ListBox>
    </Grid>
</Window>
